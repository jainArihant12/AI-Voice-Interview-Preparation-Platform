# Production / vite preview: API root (see frontend/src/services/api.js).
# Local `npm run dev`: VITE_API_URL is IGNORED — requests use /api (Vite proxy). Override only with:
# VITE_FORCE_API_URL=http://127.0.0.1:5000
